formatters:
  enable:
    - gofmt
    - gofumpt
    - goimports
    - swaggo
linters:
  enable:
    - asciicheck
    - bidichk
    - bodyclose
    - durationcheck
    - errcheck
    - fatcontext
    - godoclint
    - govet
    - ineffassign
    - misspell
    - nakedret
    - nolintlint
    - predeclared
    - reassign
    - sqlclosecheck
    - staticcheck
    - testifylint
    - tparallel
    - unconvert
    - unused
    - wastedassign
    - whitespace
    # - wrapcheck
    # - unparam
    - usestdlibvars
    - perfsprint
    - prealloc
    # - revive
    # - nestif
    # - nilerr
    - nilnil
    # - noctx
    - intrange
    - makezero
    - modernize
    # - gochecknoinits
    - gocritic
    # - gocyclo
    # - gosec
    - errname
    - errorlint
    - exhaustive
    - copyloopvar
    # - dupl
  settings:
    exhaustive:
      # A `default:` case that handles remaining enum values counts as exhaustive,
      # so switches with an explicit fallback need not list every constant.
      default-signifies-exhaustive: true
version: 2
