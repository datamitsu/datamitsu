exit code: 1
===STDOUT===
╭<RULE>╮
│ datamitsu  <VERSION> │
╰<RULE>╯

┏━ lint <RULE>
┃ pkg
┃
┃ ✗ claim  <DUR> (1 failed)
  ┌─ claim [per-project] (run #1) <RULE>
  │  Dir:       pkg/<FIRST>
  │  Cwd:       <TMP>/pkg/<FIRST>
  │  Command:   sh -c sleep 0.01; if [ -e "$MARKERS/$0" ]; then echo "$0 ${PWD##*/} later" >> "$MARKERS/$0"; exit 0; fi; echo "$0 ${PWD##*/} first" >> "$MARKERS/$0"; echo "$0: failed"; exit 1 claim
  │  Exit code: 1
  │  Duration:  <DUR>
  │
  │  claim: failed
  └<RULE>

┗━ 1 tools · 1 runs · done in <DUR> · 1 failed · cache 0% <RULE>

===STDERR===
error: operation failed
