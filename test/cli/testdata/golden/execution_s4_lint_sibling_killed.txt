exit code: 1
===STDOUT===
╭<RULE>╮
│ datamitsu  <VERSION> │
╰<RULE>╯

┏━ lint <RULE>
┃ pkg
┃
┃ ✗ alpha  <DUR> (1 failed)
  ┌─ alpha [per-project] (run #1) <RULE>
  │  Dir:       pkg/a
  │  Cwd:       <TMP>/pkg/a
  │  Command:   sh -c sleep 0.01; echo "$0 $1" >> "$MARKERS/$0"; i=0; while [ ! -f "$MARKERS/beta" ] && [ $i -lt 40 ]; do sleep 0.05; i=$((i+1)); done; echo "$0: failed"; exit 1 alpha
  │  Exit code: 1
  │  Duration:  <DUR>
  │
  │  alpha: failed
  └<RULE>

┗━ 1 tools · 1 runs · done in <DUR> · 1 failed · cache 0% <RULE>

===STDERR===
error: operation failed
