# RubyGems

> Install datamitsu via gem

# RubyGems (gem)

Install with gem:

```bash
gem install datamitsu
```

Or add to your Gemfile:

```ruby
gem "datamitsu"
```

Then run:

```bash
bundle install
```
